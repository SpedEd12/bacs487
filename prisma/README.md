# prisma (database)
Prisma models and migrations.

## Files
- `schema.prisma` (data models)
- `migrations/` (auto-generated)

## Frontend connection
Defines the data the UI can display (users, listings, messages, etc.).
