# .gitignore

This file specifies which files and directories Git should ignore when committing code.

## What it’s for
The `.gitignore` file ensures that unnecessary or sensitive files (like `node_modules/` and `.env`) are not included in version control.

## Frontend connection
Keeps the repository clean so that only relevant code and config files are shared with the frontend team.
