# BearExchange Backend
Node.js + TypeScript + Express API with PostgreSQL (Prisma). This is the main project folder.

## What it’s for
Top-level docs, scripts, and config that the whole team uses.

## Frontend connection
Frontend calls this API at `http://<host>:<port>` (see `.env.example` → `PORT`, `CORS_ORIGIN`).
