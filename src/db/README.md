# src/db
Database access helpers.

## What it’s for
- `prisma.ts`: Prisma client (DB connection)
- `seed.ts`: optional dev data seeding

## Frontend connection
Provides the data shown in the UI (users, listings, messages) through API endpoints.
