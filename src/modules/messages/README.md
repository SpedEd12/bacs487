# messages (buyer–seller chat)
Send and fetch conversations related to listings.

## Files
- `message.route.ts` / `message.controller.ts` / `message.service.ts`

## Frontend connection
`item.html` “Contact Seller”, `user.html` Conversations → `POST /messages`, `GET /messages/thread/:userId`.
