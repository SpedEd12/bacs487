# uploads (images)
Upload listing images (local or cloud).

## Files
- `upload.route.ts` / `upload.controller.ts`

## Frontend connection
Listing create/edit forms upload photos first → `POST /uploads` returns URLs used in `/listings`.
