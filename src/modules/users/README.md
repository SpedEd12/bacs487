# users (profiles & my listings)
Current user profile and their listings.

## Files
- `user.route.ts` / `user.controller.ts` / `user.service.ts`

## Frontend connection
`user.html` → `GET /users/me`, `GET /users/me/listings`, `PATCH /users/me`.
