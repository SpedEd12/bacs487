# preferences (user settings)
Read/update user preferences (e.g., theme, email opt-in).

## Files
- `preference.route.ts` / `preference.controller.ts` / `preference.service.ts`

## Frontend connection
Settings tab on `user.html` → `GET/PATCH /preferences/me`.
