# admin (moderation & overview)
Admin-only controls for listings, users, reports, and overview metrics.

## Files
- `admin.route.ts` (mounts subroutes)
- `overview.route.ts` / `.controller.ts`
- `listings.route.ts` / `.controller.ts`
- `users.route.ts` / `.controller.ts`
- `reports.route.ts` / `.controller.ts`

## Frontend connection
Admin dashboard pages → `/admin/overview`, `/admin/listings`, `/admin/users`, `/admin/reports`.
