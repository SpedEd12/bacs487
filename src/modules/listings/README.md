# listings (browse & manage items)
Create, read, update, delete, and search marketplace listings.

## Files
- `listing.route.ts` / `listing.controller.ts` / `listing.service.ts`

## Frontend connection
`index.html`, `listings.html`, `item.html` → `GET /listings`, `GET /listings/:id`, `POST/PATCH/DELETE /listings`.
