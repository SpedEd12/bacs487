# saved-searches
Store and rerun saved listing filters/queries.

## Files
- `savedsearch.route.ts` / `savedsearch.controller.ts` / `savedsearch.service.ts`

## Frontend connection
Saved Searches UI → `GET/POST/DELETE /saved-searches`, `POST /saved-searches/:id/run`.
