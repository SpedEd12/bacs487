# orders (optional purchase flow)
Stubs for future checkout/order tracking.

## Files
- `order.route.ts` / `order.controller.ts` / `order.service.ts`

## Frontend connection
If “Purchase” becomes real: `POST /orders`, `GET /orders/:id`.
