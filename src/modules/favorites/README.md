# favorites (saved items)
Save/unsave listings and list saved items.

## Files
- `favorite.route.ts` / `favorite.controller.ts` / `favorite.service.ts`

## Frontend connection
Hearts/bookmarks and Saved Items on `user.html` → `GET/POST/DELETE /favorites`.
