# views (recently viewed)
Track and list recently viewed listings.

## Files
- `viewevent.route.ts` / `viewevent.controller.ts` / `viewevent.service.ts`

## Frontend connection
`item.html` posts a view event; `user.html` shows `GET /view-events/recent`.
