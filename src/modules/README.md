# src/modules
Feature folders. Each module = router + controller + service.

## What it’s for
Keeps code organized by feature (auth, listings, messages, etc.).

## Frontend connection
Each module exposes the endpoints the frontend calls (see subfolder READMEs).
