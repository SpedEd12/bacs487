# utils (small helpers)
Shared utilities used across modules.

## Files
- `http.ts` (consistent success/error responses)
- `jwt.ts` (sign/verify tokens)

## Frontend connection
Keeps API responses predictable so the frontend can parse them easily.
