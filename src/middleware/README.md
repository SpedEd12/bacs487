# middleware (guards & validation)
Reusable request guards and error handling.

## Files
- `authGuard.ts` (requires login)
- `adminGuard.ts` (requires admin role)
- `validate.ts` (Zod schemas)
- `errorHandler.ts` (standard error JSON)

## Frontend connection
Ensures protected pages work correctly and errors are consistent for UI to display.
