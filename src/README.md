# src
All application source code.

## What it’s for
Holds the Express app, routes, business logic, and utilities.

## Frontend connection
All endpoints used by the frontend are mounted from files in here (via `app.ts`).
