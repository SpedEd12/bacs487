# package.json

This file contains metadata about the project, including its dependencies, scripts, and version.

## What it’s for
The `package.json` file defines all npm dependencies, scripts for starting and building the application, and various settings for the backend API.

## Frontend connection
The `package.json` file contains the scripts (like `npm run dev`) that start the API, which the frontend will call to fetch data.
