# tsconfig.json

This file contains TypeScript configuration options for compiling the backend code.

## What it’s for
The `tsconfig.json` file sets the TypeScript compiler’s behavior, ensuring type safety and proper handling of code when converting from TypeScript to JavaScript.

## Frontend connection
Ensures that the backend code is compiled properly and exposes APIs with stable contracts, which the frontend depends on for seamless integration.
